services:
  frontend:
    image: ghcr.io/robinnogues/my-cv-frontend:latest
    container_name: cv_frontend
    restart: unless-stopped
    volumes:
      - ./content.yaml:/src/data/content.yaml:ro
    environment:
      - DOMAIN=${DOMAIN}

  backend:
    image: ghcr.io/robinnogues/my-cv-backend:latest
    container_name: cv_backend
    restart: unless-stopped
    environment:
      - EMAIL=${EMAIL}
      - EMAIL_PASS=${EMAIL_PASS}
      - DOMAIN=${DOMAIN}
      - SMTP_SERVER=${SMTP_SERVER}
      - SMTP_PORT=${SMTP_PORT}

  caddy:
    image: caddy:alpine
    container_name: cv_caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      - DOMAIN=${DOMAIN}
      - EMAIL=${EMAIL}
    depends_on:
      - frontend
      - backend
      - goatcounter

  goatcounter:
    image: arp242/goatcounter
    container_name: cv_goatcounter
    restart: unless-stopped
    volumes:
      - goatcounter_data:/home/goatcounter/goatcounter-data
    environment:
      - GOATCOUNTER_LISTEN=:8080
      - GOATCOUNTER_AUTOMIGRATE=true
      # Domain for goatcounter might be needed but keeping it minimal as per current setup

volumes:
  caddy_data:
  caddy_config:
  goatcounter_data:
