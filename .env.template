# Domain configuration
DOMAIN=example.com

# Email configuration (used by backend for contact form, Caddy for Let's Encrypt)
EMAIL=your@email.com
EMAIL_PASS=your_app_password

# SMTP configuration (optional, defaults are for Gmail)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
